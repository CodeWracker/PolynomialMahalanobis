[tool.uv.workspace]
members = ["packages/*", "apps/*"]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "pytest>=7.0",
    "pytest-timeout",
    "matplotlib",
    "opencv-python",
    "scikit-learn",
    "zarr",
    "xxhash",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
